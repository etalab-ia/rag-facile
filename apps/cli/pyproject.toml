[project]
name = "cli"
version = "0.1.0"
description = "RAG Kit CLI"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "typer>=0.12.0",
]

[project.scripts]
rag-cli = "cli.main:app"

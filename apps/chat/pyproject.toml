[project]
name = "chat"
version = "0.1.0"
description = "ChainLit Chat Interface"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chainlit>=1.3.0",
]

[project.scripts]
chat = "chainlit.cli:run"
